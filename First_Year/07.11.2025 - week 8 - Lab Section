#Lists / Tuples / Dictionaries:
'''
l1 = list()
l2 = []

l3 = [1, 3.6, "dog"]  #l3[2] -> "dog"
l3.append(9)    #Add new element at the end of the list
l3.insert(0, 18)    #Insterts a new element into desired index. The original element is moved +1 index
l3.remove("dog")    #Self explanatory
l3.pop(1)       #Removes element with desired index (in our case, that would be '3.6')

l4 = [1, 17, 5, 31, 50, 6]
l4.sort()    #By default, sorts lowest to highest
l4.sort(reverse = True)   #Sorts highest to lowest
min(l4)      #Outputs the lowest number in our list
max(l4)      #Outputs the highest number in our list
sum(l4)      #Outputs the sum of our list
len(l4)      #Outputs the length of our list
count(l4)   


t1 = tuple()    #Кортеж... or whatever this means
t2 = (1, 2, 3)


for i in l4:
    print(i)    #Prints all the elements

for i in range (len(l4)):
    if i % 2 == 1
        l4.pop(i)   /or/  remove(l4[i])

#Dictionary
d1 = dict()
#{key : value; ...}
d2 = {"dog":1; 3:2;}    #d2["dog"] -> 1
d2["cat"] = 18      #This way we add a new element in our dictionary
d2["dog"] = 11      #Now "dog" has the value of '11'


l6 = [1, 1, 1, 3, 1, 5]
s1 = set(l6)
print(s1)   #Will return the unique elements like: 1, 3, 5 /or/ 5, 1, 3 /or/ 5, 3, 1 /or/ 1, 5, 3 /and so on (if I forgot something)/

'''
